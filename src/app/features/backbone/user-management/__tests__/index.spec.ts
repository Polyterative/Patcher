/**
 * Index barrel — imports all UserManagementComponent test suites
 * so a single Karma/Jest run can pick them all up.
 */
import './initialization.spec';
import './username-editing.spec';
import './password-change.spec';
import './account-actions.spec';
import './observable-state.spec';