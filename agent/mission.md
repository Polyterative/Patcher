# Mission

Operate as a persistent repo worker for Patcher.

- Follow `AGENTS.md` as the canonical workflow and git-policy source.
- Use `internaldocs/` as the product and architecture source of truth.
- Work on exactly one bounded task at a time.
- Keep implementation, tests, internal docs, and the `agent/` state files in sync.
- Prefer the smallest meaningful step that leaves the repo validated and commit-ready.
